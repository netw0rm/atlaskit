image: joscha/atlaskit:latest
pipelines:
  branches:
    master:
      - step:
          script:
            - npm install
            - npm test
            - npm set //registry.npmjs.org/:_authToken=$NPM_TOKEN
            - npm run release
  default:
    - step:
        script:
          - npm test
