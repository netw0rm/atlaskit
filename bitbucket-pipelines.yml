image: node:5.11.0
pipelines:
  default:
    - step:
        script:
          - npm --version
          - npm install
          - npm set //registry.npmjs.org/:_authToken=$NPM_TOKEN
          - npm run release