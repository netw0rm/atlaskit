image: node:5.11.0
pipelines:
  default:
    - step:
        script:
          - npm --version
          - npm install
          - npm run release
          - expect "Are you sure you want to publish the above changes? (ynH)"
          - send "y"