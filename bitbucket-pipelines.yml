image: node:5.11.0
pipelines:
  branches:
    - master:
      - step:
          script:
            - npm --version
            - npm install
            - npm set //registry.npmjs.org/:_authToken=$NPM_TOKEN
            - npm run release
  default:
    - step:
        script:
          - npm test