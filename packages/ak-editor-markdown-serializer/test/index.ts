import markdownSerializer from '../src/index';

describe('nodes', () => {
  it('should serialize a blockquote', () => {
  });

  it('should serialize a code block', () => {
  });

  it('should serialize headings (level 1 - 6)', () => {
  });

  it('should serialize horizontal_rule', () => {
  });

  it('should serialize bullet list', () => {
  });

  it('should serialize ordered list', () => {
  });

  it('should serialize paragraph', () => {
  });

  it('should serialize image', () => {
  });

  it('should serialize hard_break', () => {
  });

  it('should serialize text', () => {
  });
});

describe('marks', () => {
  it('should serialize em', () => {
  });

  it('should serialize strong', () => {
  });

  it('should serialize link', () => {
  });

  it('should serialize code', () => {
  });
});

describe('mixed content', () => {
  it('should serialize dragons and demons', () => {
  });
});
