<!DOCTYPE html>
<html>
    <head>
      <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
      <title><%= htmlWebpackPlugin.options.title %></title>
    </head>
    <body>
      <div style="width:300px; margin:0px auto 100px auto; border:1px solid red; position:relative;height:300px;" id="target">
        test
        <br>
        <button id="button">Super button</button>
        <br/>
        <style>
          ak-inline-dialog {
            display:none;
          }

        </style>
        <ak-inline-dialog>test inline dialog2</ak-inline-dialog>

        <script>
          var dialog = document.querySelector("ak-inline-dialog");
          dialog.target = '#target';
          dialog.position = 'left middle';
          //dialog.open = true;

          var button = document.querySelector('#button');
          button.addEventListener('click', function () {
            dialog.open = !dialog.open;
          })
        </script>
      </div>
    </body>
</html>
