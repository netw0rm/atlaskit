// ak-tabs symbols

const buttonContainer = Symbol();
const focusOnRender = Symbol();
const focusOnSecondRender = Symbol();
const labelsContainer = Symbol();

// ak-tabs-tab symbols

const tabLabel = Symbol();

export {
  buttonContainer,
  focusOnRender,
  focusOnSecondRender,
  labelsContainer,
  tabLabel,
};
