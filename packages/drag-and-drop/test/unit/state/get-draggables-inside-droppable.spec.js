// @flow
import { describe, it } from 'mocha';

describe('get draggables inside a droppable', () => {
  const droppable: Dimension = {
    id: 'drop-1',
  };

  const inside: Dimension[] = [

  ];

  it('should only return dimensions that are inside a droppable', () => {

  });
});
