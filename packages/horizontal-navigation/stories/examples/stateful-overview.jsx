import React, { PureComponent } from 'react';
import AkHorizontalNavigation from '@atlaskit/horizontal-navigation';

export default class extends PureComponent {
  render() {
    return (
      <AkHorizontalNavigation label="Stateful toggle" />
    );
  }
}
