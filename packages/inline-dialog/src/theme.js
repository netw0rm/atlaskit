import { colors, themed } from '@atlaskit/theme';

export const backgroundColor = themed({
  light: colors.N0,
  dark: colors.DN50,
});

export const borderColor = themed({
  light: colors.N60A,
  dark: colors.DN50A,
});

export const shadowColor = themed({
  light: colors.N50A,
  dark: colors.DN50A,
});
