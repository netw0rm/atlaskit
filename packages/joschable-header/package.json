{
  "name": "joschable-header",
  "version": "1.3.0",
  "description": "",
  "main": "dist/bundle.js",
  "scripts": {
    "clean": "rm -rf node_modules",
    "dev-server": "webpack-dev-server --bundle-deps",
    "prepublish": "cd .. && npm run prepublish/single joschable-header",
    "pre-release": "lerna-semantic-release pre",
    "test": "../../node_modules/.bin/karma start ../../karma.conf.js --single-run",
    "test/watch": "../../node_modules/.bin/karma start ../../karma.conf.js"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sean-button": "^3.0.0"
  }
}
