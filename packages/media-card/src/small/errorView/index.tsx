import * as React from 'react';

export interface SmallErrorViewProps {
}

export const SmallErrorView = ({}: SmallErrorViewProps): JSX.Element => ( // tslint:disable-line:variable-name
  <div></div>
);
