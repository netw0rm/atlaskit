import * as React from 'react';
import {MediaItemDetails} from '@atlaskit/media-core';

export interface SmallFileViewProps {
  metadata?: MediaItemDetails;
}

export const SmallFileView = ({metadata}: SmallFileViewProps): JSX.Element => ( // tslint:disable-line:variable-name
  <div></div>
);
