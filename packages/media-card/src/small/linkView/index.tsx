import * as React from 'react';
import {MediaItemDetails} from '@atlaskit/media-core';

export interface SmallLinkViewProps {
  metadata?: MediaItemDetails;
}

export const SmallLinkView = ({metadata}: SmallLinkViewProps): JSX.Element => ( // tslint:disable-line:variable-name
  <div></div>
);
