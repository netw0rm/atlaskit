import styled from 'styled-components';

export const ProgressTrackerContainer = styled.div`
    display: flex;
    width: 100%;
    margin-top: 40px; // The bar should be aligned to the icon, sitting 48px from the top. 
`;

export default ProgressTrackerContainer;
