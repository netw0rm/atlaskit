import { colors } from '@atlaskit/theme';
import styled from 'styled-components';

export const ProgressTrackerLink = styled.a`
    cursor: pointer;
    color: ${colors.N800};
`;

export default ProgressTrackerLink;
