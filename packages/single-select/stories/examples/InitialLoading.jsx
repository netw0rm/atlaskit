import React from 'react';
import { StatelessSelect } from '../../src';

export default (
  <StatelessSelect
    items={[]}
    isOpen
    isLoading
    loadingMessage="Custom loading message"
    shouldFitContainer
  />
);
