import { colors, themed } from '../../theme/src';

export const tooltipBackgroundColor = themed({
  light: colors.N800,
  dark: colors.DN80,
});
export const tooltipTextColor = themed({
  light: colors.N0,
  dark: colors.DN800,
});
