import { colors, themed } from '../../theme/src';

export const backgroundColor = themed({
  light: colors.N800,
  dark: colors.DN80,
});

export const textColor = themed({
  light: colors.N0,
  dark: colors.DN800,
});
