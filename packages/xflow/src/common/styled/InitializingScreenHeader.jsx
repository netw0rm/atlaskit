import styled from 'styled-components';

const InitializingScreenHeader = styled.div`
  padding: 20px 20px 14px;
`;

InitializingScreenHeader.displayName = 'InitializingScreenHeader';
export default InitializingScreenHeader;
