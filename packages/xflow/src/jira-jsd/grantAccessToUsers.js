import grantAccessToUsers from '../common/grantAccessToUsers';

// TODO update for JSD (possibly not quite as simple as a user-group
export default grantAccessToUsers('confluence-users', 'confluence');
