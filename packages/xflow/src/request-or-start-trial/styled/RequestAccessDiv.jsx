import styled from 'styled-components';

import { colors } from '@atlaskit/theme';

const RequestAccessDiv = styled.div`
  color: ${colors.N800}
`;

RequestAccessDiv.displayName = 'RequestAccessDiv';
export default RequestAccessDiv;
