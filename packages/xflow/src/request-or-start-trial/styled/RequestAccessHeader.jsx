import styled from 'styled-components';

const RequestAccessHeader = styled.div`
  display: inline-flex;
  align-items: center;
`;

RequestAccessHeader.displayName = 'RequestAccessHeader';
export default RequestAccessHeader;
