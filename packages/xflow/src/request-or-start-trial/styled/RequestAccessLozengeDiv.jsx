import styled from 'styled-components';

import { gridSize, math } from '@atlaskit/theme';

const RequestAccessLozengeDiv = styled.div`
  margin-left: ${math.multiply(gridSize, 1.5)}px;
`;

RequestAccessLozengeDiv.displayName = 'RequestAccessLozengeDiv';
export default RequestAccessLozengeDiv;
